[package]
name = "aichain-risc0-policy-evaluation-guest"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
aichain-zk-policy-core = { path = "../../../sp1-policy-evaluation/lib" }
risc0-zkvm = { version = "=3.0.3", default-features = false, features = ["std"] }
